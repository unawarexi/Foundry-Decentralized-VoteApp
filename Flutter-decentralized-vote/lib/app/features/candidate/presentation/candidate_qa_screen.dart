import 'package:flutter/material.dart';

class CandidateQaScreen extends StatelessWidget {
  const CandidateQaScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('CandidateQaScreen')));
}
