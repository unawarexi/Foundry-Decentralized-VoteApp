import 'package:flutter/material.dart';

class RegionMapScreen extends StatelessWidget {
  const RegionMapScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('RegionMapScreen')));
}
