import 'package:flutter/material.dart';

class IdScanScreen extends StatelessWidget {
  const IdScanScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('IdScanScreen')));
}
