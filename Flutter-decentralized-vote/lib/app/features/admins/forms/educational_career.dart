import 'package:flutter/material.dart';

class EducationalCareer extends StatelessWidget {
  const EducationalCareer({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('EducationalCareer')));
}
