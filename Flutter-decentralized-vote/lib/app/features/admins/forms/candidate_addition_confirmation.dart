import 'package:flutter/material.dart';

class CandidateAdditionConfirmation extends StatelessWidget {
  const CandidateAdditionConfirmation({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('CandidateAdditionConfirmation')));
}
