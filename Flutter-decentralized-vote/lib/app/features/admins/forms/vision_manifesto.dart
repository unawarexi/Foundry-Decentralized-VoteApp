import 'package:flutter/material.dart';

class VisionManifesto extends StatelessWidget {
  const VisionManifesto({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('VisionManifesto')));
}
