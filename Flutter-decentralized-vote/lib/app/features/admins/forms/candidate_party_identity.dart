import 'package:flutter/material.dart';

class CandidatePartyIdentity extends StatelessWidget {
  const CandidatePartyIdentity({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('CandidatePartyIdentity')));
}
