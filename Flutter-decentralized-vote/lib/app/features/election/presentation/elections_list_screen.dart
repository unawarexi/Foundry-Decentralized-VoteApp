import 'package:flutter/material.dart';

class ElectionsListScreen extends StatelessWidget {
  const ElectionsListScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('ElectionsListScreen')));
}
