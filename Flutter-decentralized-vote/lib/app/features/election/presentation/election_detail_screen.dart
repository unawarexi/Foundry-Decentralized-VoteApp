import 'package:flutter/material.dart';

class ElectionDetailScreen extends StatelessWidget {
  const ElectionDetailScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('ElectionDetailScreen')));
}
